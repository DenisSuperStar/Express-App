<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <!-- Подключаем стили компонентов в нашем частичном представлении head.hbs -->
    <!-- Через теги link не подключать, это не поддерживается -->
    <style>
        /*
            Стили для всей страницы
        */
        .page {
            height: 100vh;
            background: linear-gradient(180deg, #b968c5 0, skyblue 100%);
        }

        .page__body {
            padding: 0 140px 0 30px;
        }

        .page__grid {
            display: flex;
            justify-content: space-between;
        }

        .page__grid-box-1 {
            display: flex;
            flex-direction: column;
        }

        .page__title-box {
            padding: 0 0 15px 0;
        }

        .page__player-box {
            display: flex;
            padding: 0 0 14px 0;
        }

        /*
            Стили компонента navbar
        */
        .navbar {
            background: #000;
            padding: 35px 0;
        }

        .navbar__nav-box {
            padding: 0 0 0 30px;
        }

        .navbar__nav-box,
        .navbar__list {
            display: flex;
            justify-content: space-between;
        }

        .navbar__brand,
        .navbar__nav-link {
            text-decoration: none;
        }

        .navbar__brand-text {
            color: #fff;
            font-size: 20px;
            text-transform: uppercase;
        }

        .navbar__collapse {
            padding: 0 140px 0 0;
        }

        .navbar__nav-link {
            color: #fff;
            font-size: 15px;
            text-transform: uppercase;
            font-weight: bold;
        }

        .navbar__list {
            list-style: none;
            margin: 0;
            padding: 0;
        }

        .navbar__list > .navbar__nav-item {
            padding: 0 20px 0 0;
        }

        .navbar__list > .navbar__nav-item:last-child {
            padding: 0;
        }

        /*
            Стили компонента sidebar
        */
        .sidebar-menu__menu-box {
            padding: 0 0 0 30px;
        }

        .sidebar-menu__list {
            list-style: none;
            margin: 0;
            padding: 40px 0 22px 0;
        }

        .sidebar-menu__list > .sidebar-menu__item:last-child {
            padding: 0;
        }

        .sidebar-menu__item {
            padding: 0 0 10px 0;
        }

        .sidebar-menu__link {
            text-decoration: none;
            font-size: 14px;
            color: #fff;
        }

        /*
            Описание стилей компонента title
        */
        .title {
            color: #000;
            font-size: 15px;
            text-transform: uppercase;
            font-weight: bold;
        }

        /*
            Описание стилей компонента Список Жанров
        */
        .genre-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .genre-list__item {
            padding: 0 0 10px 0;
        }

        .genre-list__link {
            text-decoration: none;
            color: #fff;
            font-size: 15px;
            text-transform: uppercase;
            font-weight: bold;
        }
    </style>

</head>